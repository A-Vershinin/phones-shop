// const SET_BOOKS = 'SET_BOOKS';

const initialState = {

};

const phonesReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default phonesReducer;
